Flask
selenium
# Core dependencies for the refactored Python modules

requests>=2.25.0
beautifulsoup4>=4.9.0
lxml>=4.6.0
webdriver-manager>=3.5.0 # For easier Selenium driver management
googlesearch-python>=1.0.1 # For Google dorking in NetworkUtilities
tabulate>=0.8.9 # For potentially formatting output in CLI

werkzeug
Flask-Cors

SQLAlchemy
psycopg2-binary

gunicorn

# Note: Specific versions are examples. Adjust as needed for compatibility.
# Some OSINT/Network utilities might require additional libraries if their
# underlying API wrappers are used directly or expanded.
# For example, if direct interaction with specific social media APIs (beyond Facebook Graph API)
# were added, those SDKs would be listed here.
